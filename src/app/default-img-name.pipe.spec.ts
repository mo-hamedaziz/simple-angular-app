import { DefaultImgNamePipe } from './default-img-name.pipe';

describe('DefaultImgNamePipe', () => {
  it('create an instance', () => {
    const pipe = new DefaultImgNamePipe();
    expect(pipe).toBeTruthy();
  });
});
