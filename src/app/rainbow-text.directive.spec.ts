import { RainbowTextDirective } from './rainbow-text.directive';

describe('RainbowTextDirective', () => {
  it('should create an instance', () => {
    const directive = new RainbowTextDirective();
    expect(directive).toBeTruthy();
  });
});
